[project]
name = "griddy-nfl"
version = "0.9.2"
description = "NFL SDK for the Griddy platform – typed access to NFL, Pro, and Next Gen Stats APIs."
authors = [
    {name = "John Griebel", email = "john@thistlegrow.com"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "griddy-core",
    "httpx==0.28.1",
    "pydantic>=2.0.0",
    "requests>=2.28.0",
    "playwright==1.55.0",
]

[tool.uv.sources]
griddy-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/griddy_nfl"]
